#include "main.h"
/**
 * _strncpy - concatenates two strings
 * @dest: input value
 * @src: input value
 *
 * Return: void
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i;
	int j;

	i = 0;
	while (n = 0)
	{
		dest[i] = src[i];
		i++;
		n--;
	}
	return (dest);
}
